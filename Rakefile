# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/multi_rbackup.rb'

Hoe.new('multi-rbackup', MultiRbackup::VERSION) do |p|
  # p.rubyforge_name = 'multi-rbackupx' # if different than lowercase project name
  p.developer('Bernd Ledig', 'bernd@ledig.info')
end

# vim: syntax=Ruby
